# TODO 4（模型流可视化）调研分析报告

> 本轮为调研规划轮，只输出结论与计划，未改任何代码。所有结论均经代码逐行核验，附文件:行号证据。
> 涉及文件：`segtask_v1/visualization/{model_flow.py, graph.py, render.py, __init__.py}`、`segtask_v1/train.py`、`tests/test_model_flow.py`、`segtask_v1/models/{edm2_unet.py, blocks.py}`。

---

## 一、TODO.md「审查」结论逐条核验

### 1. P1 —— `_tl_trace` 前向污染真实训练模型状态：**成立，且比审查描述更具体**

证据链：
- `train.py:95-101`：`model = build_model(cfg)` 后直接把**同一个将被训练的 model** 传给 `generate_visualization(cfg, model)`，无 deepcopy。
- `model_flow.py:169-173`：`_tl_trace` 先 `model.train(True)` 再前向。`torch.no_grad()` 只阻断 autograd，**不阻断**：
  1. **BN running stats**：train 模式下所有 BatchNorm 的 `running_mean/var` 被全零 dummy 输入更新一次（`momentum=0.1` 默认下即被拉向 0 的统计），`num_batches_tracked` +1。虽然一次前向影响有限，但这是对"尚未开始训练"的模型的确定性污染。
  2. **EDM2 `_MPConv` 强制 weight-norm**：`edm2_unet.py:78-80`，train 模式前向会 `self.weight.copy_(_normalize(w))` **就地改写参数**。对 EDM2 这是设计上幂等的（后续训练本来也会做），但发生时点提前到了训练前，且是被"可视化"这一只读功能触发——违反"生成过程……不读盘、不依赖 GPU"的只读承诺（`__init__.py:5`）。
- `model_flow.py:181` 只恢复了 `training` 标志，**未恢复 buffers/参数**。

结论：审查判定正确。任何"有训练期副作用的 forward"（BN、EDM2 norm、dropout 的 RNG 消耗——后者还会**改变全局随机数序列**，影响可复现性）都会泄漏到真实训练。修复方向：对 `copy.deepcopy(model)`（CPU 上代价可接受）做追踪，或退而求其次备份/恢复全部 `named_buffers` + 受影响参数（deepcopy 更通用、无特判，推荐）。

### 2. P2 —— 乘法门控被误判为 residual：**成立，且边标签还有伴生 bug**

证据链：
- `model_flow.py:110`：`_ADDITIVE_SYMBOLS = {"+", "−", "×", "lerp"}`，`×` 被纳入"加性"融合参与残差判定。
- `blocks.py:1002-1035` `AttentionGate3D.forward`：`return x * self.psi(self.relu(self.W_x(x) + self.W_g(g)))`。`x` 同时是乘法的一路输入、又经 `W_x` 可达 `psi` 一路 ⇒ 触发规则 1（`model_flow.py:365-370` "a 可达 b ⇒ a 是恒等捷径"），`x → ×` 被标 residual。
- 伴生 bug：`model_flow.py:696` 对所有 residual 边硬编码标签 `"+"`——门控乘法的捷径边会被画成橙色虚线并标注 `+`，**语义双重错误**（既不是残差、更不是加法）。
- 语义上：residual 的判定依据（"可达另一路"或"显著更短"）只对**加法**成立——`y = f(x) + x` 中 x 是恒等捷径；`y = x·g(x)` 中 x 是被门控的主信号，g 才是调制路。SE block、attention gate、GLU 均属此类。

结论：审查判定正确。修复方向：把 `×`（及 `lerp`？lerp(a,b,w) 语义近似加权和，可保留）移出 `_ADDITIVE_SYMBOLS`，为乘法门控引入独立 `gate` 边类/或保持普通 forward + merge 符号 `×`；同时把 `:696` 的标签改为随 merge 符号而非硬编码 `+`。

### 3. P2 —— rank/col 双真相源：**成立（builder 计算的 rank/col/colspan 仅测试消费，渲染器完全无视）**

证据链：
- builder 侧：`model_flow.py:720-721` 调 `_assign_ranks_and_skip(g)`（Kahn 最长路径，`:760-895`）+ `assign_grid_layout(g)`（`graph.py:155-231` 列分配），把 rank/col/colspan 写进 VisNode，且 `graph.py:44-50` 的文档声称"renderer 据此把节点摆进 CSS Grid"。
- renderer 侧：全 `render.py` 只读了 `node.res`（`:592`），**从未读取** `rank/col/colspan`；JS 在 `layerRanks`（`:452-494`）里用几乎相同的 Kahn 最长路径**重算**分层，列位则由脊柱 + barycenter（`:637-696`）另行决定。
- 后果：① `graph.py` 文档撒谎（维护误导）；② 两套算法的断环/并列细节可能分歧，builder 侧的 skip 标注（依据投影 rank 跨度，`:889-895`）与 renderer 实际布局可能不一致；③ builder 的 `assign_grid_layout` 及其复杂度纯属死重（唯一消费者是 `test_model_flow.py:315-344` 两个测试）。

结论：审查判定正确。修复方向二选一：(a) 删除 builder 侧 rank/col 计算，skip 判定改用纯结构规则（`_has_alt_path` 已不依赖 rank，只有 `跨度>1` 一条依赖），测试改测 IR 结构不测格位；(b) renderer 改为消费 IR 的 rank。推荐 (a)——renderer 的布局远比网格模型精细（换行/挂靠/车道），回不去 CSS Grid 语义；单真相源应设在真正做布局的一侧，IR 只承载**结构**（DAG、层级、res），不承载**几何**。注意 `n.rank` 仍被 `_assign_ranks_and_skip` 内部（skip 跨度判定、head/loss 拍平）使用，需一并梳理保留最小集。

### 4. P2/P3 次要项：**全部成立**

| 项 | 核验 | 备注 |
|---|---|---|
| CPU dummy 全尺寸前向代价 | 成立。`model_flow.py:558` 只把 batch 固定为 1，空间尺寸取全量 patch（3D 大 patch 上 CPU 前向可达分钟级）。对照 `test_model_flow.py:36-42` 测试自己都把 3D patch 缩到 1/8 | 可选 P3：`trace_shapes` 之外提供缩尺追踪（注意 res 缩进以输入 W 为基准，等比缩小不影响 `_res_level`；但对含绝对位置编码/固定尺寸假设的模型需回退全尺寸） |
| `cat(x,x)` 去重 | 成立。`contract()` 中 `up` 是 set（`:279-281`），同源两路收缩为 1 ⇒ `len(up)>=2` 不满足（`:299`），merge 节点消失、退化为透传；即便成 merge，`_add_cedge` 去重（`:310-313`）也只画一条边，通道翻倍不可见 | 影响小（cat(x,x) 少见），P3 |
| `_res_level` 只看末维 W | 成立（`:593-602`）。各向异性下采样（如 3D 里 D 减半 W 不变）缩进不动；W 上采样超过输入（超分/padding）回退 0 | P3。可改为对全部空间维取平均 log2 |
| functional op 不可见 | 成立。单上游 functional op 一律透传（`:307-308`）——模块外的 `F.interpolate`/`sigmoid`/切片等在图上蒸发，只有 ≥2 上游的融合才材料化 | 这是有意的抽象取舍（叶子=nn.Module）；但 `interpolate` 这类**改变形状**的 op 消失会让相邻节点 in/out 形状"对不上"，可考虑 P3：形状变化的单上游 op 也材料化为小 op 节点 |

---

## 二、对照五条原则的独立分析

### 原则 1：层次化 —— 达成度高
- 容器树来自真实 module 层级 + 单子容器上卷（`model_flow.py:389-428`），折叠默认收起（`:648`）。
- 聚焦限定 stem/stage 级：`render.py:340-344` `canFocus` = 顶层 + 顶层直接子模块，符合"聚焦不深入子模块"的要求；更深层双击看详情抽屉（`:1307`）。
- 锚点导航（`:1168-1206`）与聚焦层级一致，纯数据驱动。
- **弱点**：`_is_head_container`（`model_flow.py:605-607`）用**名字包含 "head"** 判定输出头——这是全链路里唯一一处命名特判，违反原则 5（见下）。

### 原则 2：结构化 —— 达成度高
- builder（语义/结构）与 renderer（几何）分层清晰，IR 语言无关。
- 但 **rank/col 双真相源**（上文 §3）正是结构化的裂缝：IR 里混入了没人消费的几何字段，且文档与实现背离。

### 原则 3：位置即计算次序 —— 基本达成
- 纵向：ASAP 最长路径分层，同层并列（`render.py:452-494`）；容器/叶子发射序按首次执行 step（`model_flow.py:626-630, 652`）。
- 横向：脊柱按 res 缩进呈 U 型（`:670`），旁支 barycenter 就近（`:645-649`），挂靠节点对齐"源的下一计算行"（`:606-635`）——注释明确以"位置即计算次序"为设计目标。
- **风险点**：ASAP 分层会把浅层旁支（如 deep-supervision 头）提到其最早可行层而非实际执行层；与"次序"有细微偏差，但对可读性通常是改善，不算违反。

### 原则 4：走线可溯源、不交叉 —— 机制完备，正确性靠近似
- 车道左 skip/右 residual（`:735-744`）、区间打包（`:536-549`）、折弯边成簇拓扑排序分配中线 y（`:944-975`）、扇入扇出按对侧 x 排序（`:826-840`）、穿框绕行兜底（`:977-1008`）、hover 单边溯源 + tooltip（`:1143-1158`）。设计密度很高。
- **residual 误判会直接放大到走线层**：门控乘法边被丢进右车道 + 橙色 + `+` 标签（§2），是"可溯源"原则下最扎眼的语义错误。
- 折弯 y 分配的约束成环兜底"保序"（`:965-966`）意味着极端扇形下仍可能交叉，属已知近似，可接受。

### 原则 5：通用无特判 —— 仅一处违背
- 残差/skip/分层/车道全部基于 DAG 结构与形状，无架构名特判，符合要求。
- **唯一命名特判**：`_is_head_container` 的 `"head" in name`（`model_flow.py:605`，用于框着色 + head 拍平同行 `:846-850` + 顶层→loss 连边标签 `:716`）。改名为 `decoder_out` 的头会失去待遇；反之叫 `overhead` 的容器会被误判。结构化替代：`kind=="head"` 可由「顶层容器且其输出是 `is_output` op 的 rep」推出（`:707-714` 已经在算 out_tops），不需要看名字。建议纳入本次修复（P2 末尾顺手项）。

### 额外发现（审查未列）
1. **residual 边标签硬编码 `"+"`**（`model_flow.py:696`）——即使修好 ×，`lerp` 残差也会标 `+`。应传 merge 符号。
2. **`_tl_trace` 消耗全局 RNG**：dropout 等在 train 模式前向会推进默认 RNG，`generate_visualization` 在 `seed_everything` 之后、训练之前被调用的话会改变训练随机序列（需确认 seed 时点；与 P1 同根，deepcopy + fork_rng 一并解决）。
3. **规则 2 深度阈值（≤1/2 且更短）是启发式**：投影捷径 `conv1x1` 深度 1 vs 主路深度 2 时 `1*2<=2` 成立可标注，但主路仅 2 层的浅 block 里 1 vs 2 也会误标。当前无反例架构，保持观察即可（P3，不建议现在动）。

---

## 三、优先级建议与分步计划（每步可独立执行）

### Step 1（P1）：追踪隔离——消除对训练模型的状态污染
- **做法**：`_tl_trace` 内对 `copy.deepcopy(model)` 追踪（模型此时在 CPU，deepcopy 代价 = 一份参数内存）；用 `torch.random.fork_rng()` 包裹前向隔离 RNG。deepcopy 失败（不可深拷模块）时告警回退现行为。
- **产出**：`model_flow.py` 改动 ~10 行；新增回归测试：追踪前后 `named_buffers`/`parameters`/RNG state 逐一 allclose。
- **验收**：现有 `test_model_flow.py` 全绿 + 新测试通过。
- **依赖**：无。

### Step 2（P2）：融合语义修正——× 不是残差
- **做法**：`_ADDITIVE_SYMBOLS` 移除 `×`；residual 边标签改用对应 merge 符号（`:696`）；merge 节点符号本身已正确显示 `×`，门控退化为普通 forward + × 融合点。
- **产出**：`model_flow.py` 2 处小改；新增测试：含 AttentionGate 的配置下无 `x→×` residual 边、图例语义正确。
- **验收**：`test_edm2_residuals...`、`test_resnet_block_residual` 等既有残差测试不回归。
- **依赖**：无（与 Step 1 正交）。

### Step 3（P2）：单真相源——IR 去几何化
- **做法**：删 `assign_grid_layout` 调用与 VisNode 的 col/colspan（rank 保留为 `_assign_ranks_and_skip` 的内部量或降为 debug 字段）；修正 `graph.py:44-50` 文档；`test_grid_no_column_overlap`/`test_linear_flows_no_stacking` 改为断言结构不变量（连通性、skip 数、rank 单调）或随字段删除。
- **产出**：`graph.py`/`model_flow.py`/测试联动改动。
- **验收**：渲染输出 HTML 与改动前逐字节一致（renderer 本就不读这些字段，可做黄金对比）。
- **依赖**：无，但建议排在 1、2 之后（改动面稍大）。

### Step 4（P2 顺手项）：head 判定去命名特判
- **做法**：用「顶层容器的 rep 集与 out_tops 相交」判 head，替换 `_is_head_container` 的名字匹配（结构降级路径 `_emit_structural` 无 trace 信息，可保留名字启发式并注释说明）。
- **验收**：现有 head 相关渲染（着色、同行拍平、loss 边标签）在标准配置下不变。
- **依赖**：无。

### Step 5（P3，可选，建议缓做）：次要项
- 缩尺追踪降 CPU 成本（默认关、配置开）；`_res_level` 改多维平均；形状变化的单上游 functional op 材料化；`cat(x,x)` 重复度标注。每项独立小 PR，均需先确认有真实场景收益再动，避免范围膨胀。

---

## 四、布局统一方案（模块坐标 + 走线，回应"逐级右偏"问题）

### 4.1 现状诊断：为什么大小模块都"依次往右偏"

现行 renderer 的横向定位实际上混用了**四套规则**，且 res 缩进的基准**每个容器各自重置**，两者叠加造成从顶层（enc→dec→head）到容器内（stem→stage0→stage1）的系统性右偏：

| # | 成因 | 位置 | 机理 |
|---|---|---|---|
| D1 | **res 基准每容器重置** | `render.py:591-595` | 每个容器独立算 `resMin = min(脊柱节点 res)`，缩进量 = `(res−resMin)×INDENT`（`:670`）。父容器与子容器的基准互不相同、不可比：顶层用"容器输出形状的 res"（enc 输出=瓶颈 → 大缩进；dec 输出=全分辨率 → 0），容器内又以 stem 为基准重新起步。各层级缩进互相叠加/错位，没有全局可解释的坐标含义 |
| D2 | **容器 res 取输出形状** | `model_flow.py:631-649` | 折叠的 encoder 框按其**出口**（瓶颈）res 缩进——但视觉上这个框代表整段下采样过程，按出口缩进使折叠框整体右移，与相邻框（input res0）错位；dec 框按出口回到 0，于是顶层呈现"enc 偏右、dec 归零、head 又随 barycenter 漂移"的无规律阶梯 |
| D3 | **三套定位规则混用** | `render.py:668-693, 697-707` | 脊柱行：`cx = res 缩进`；非脊柱行：前驱 barycenter **平均后居中**；挂靠节点：容器右缘外排。barycenter 平均会把已右偏节点的偏移继续向下游传播并累积（平均包含被 D1/D2 推右的前驱），逐 rank 放大 |
| D4 | **脊柱行侧枝只排右侧** | `render.py:672-678` | 同 rank 的非脊柱节点一律排在脊柱右侧，进一步把下游 barycenter 拉右 |
| D5 | **中心对齐 + 宽度不一** | `geo = cx − w/2`（`:760`） | 所有对齐都以中心 x 为准；enc/dec 框宽、head 框窄，即使中心共轴，左缘参差也造成"没对齐"的观感 |
| D6 | **换行续排行居中** | `render.py:680-693` | 超宽换行后的续排行按 barycenter 居中，与首行左缘不齐 |

结论：偏移不是某一处 bug，而是**横坐标没有统一的语义**——它混合了"分辨率缩进（局部基准）""前驱平均""右排侧枝"三种信号。统一方案的核心是给横坐标一个全局唯一的语义。

### 4.2 统一规则（推荐方案 A：全局 res 泳道 + 单一主轴）

坐标语义一句话：**纵坐标 = 计算次序，横坐标 = 分辨率级**，两条编码全局一致、逐层继承、任何层级不重置基准。

**模块布局五条规则**（R1-R5，全部只依赖 DAG 结构与追踪形状，无架构特判）：

- **R1（纵向不变）**：rank = 容器内 ASAP 最长路径分层，保持现状；纵向即计算次序。
- **R2（全局 res 泳道）**：全图建立统一的分辨率泳道，`x轴 = 主轴 + node.res × INDENT`，res 用**绝对值**（相对模型输入），**取消 per-container resMin**。子容器主轴 = 父容器主轴（坐标直接继承，不再平移归零）。效果：encoder 内 stem→stage 逐级右进、decoder 逐级左回，**跨层级严格对齐**——展开 encoder 后其内部 stage2 与顶层其他 res2 的节点落在同一泳道上；res 恒定的图（数据流/预测流）自动退化为单列直线。
- **R3（容器 res 语义修正）**：折叠容器的缩进 res 取其**入口 res**（进入该容器时的分辨率，`container_io` 的 in_sh 已有），而非出口 res——折叠框代表"从这里开始的一段计算"，按入口对位后顶层 input→enc→dec→head 折叠时是笔直一列（都从 res0 进入），展开时才展开成 U 型，符合直觉且折叠/展开语义连续。
- **R4（单一定位规则）**：废除"非脊柱行 barycenter 居中定位"。所有节点位置一律 = 所属泳道轴；barycenter 只用于**同 rank 同泳道多节点时的左右排序**（保持交叉最少的原有收益），排序后从泳道轴向右等距排开。挂靠节点保留（其纵向对齐逻辑符合"位置即计算次序"），但横向也吸附到"源泳道 + 1 个侧位"而不是容器右缘任意排。彻底消除平均值漂移与累积。
- **R5（对齐观感）**：同泳道节点**中心对齐**（泳道轴即中心线）；同容器同 kind 的叶卡统一取最大宽度；换行续排行与首行**左缘对齐**。可选：以极淡背景色/细竖线画出泳道参考线，用户可一眼校验"同一竖线 = 同一分辨率级"。

**走线统一规则**（L1-L5）：

- **L1（边语义四分）**：forward（垂直直连，灰实线）/ skip（左车道，蓝实线）/ residual（右车道，琥珀虚线）/ **feedback 上行边独立线型**（左车道但用不同 dash，与 skip 区分方向语义；现状 `:773-774` 两者混同）。若 Step 2 引入 gate，gate 走右车道、独立颜色。
- **L2（层间通道分槽）**：直连边至多 1 次折弯；折弯的中线 y 不再"簇内均匀取值"（`:969-972`），改为在层间空隙里按固定最小间距分槽（与车道同思想），保证任意扇形下折线间距 ≥ 常量。
- **L3（车道完全错开）**：引出/引入水平段偏移 = `lane 序 × 常量`，替换 `(lane%3−1)×5`（`:901`）的 3 档循环——≥4 车道时不再共线，逐线可溯源。
- **L4（gutter 与泳道对齐）**：车道 gutter 宽度全局常量，且车道 x 落在泳道网格的间隙带上，避免 gutter 挤压节点导致的二次偏移（现状 `dx = pad+gutL−minX` `:758` 会因车道数不同使兄弟容器内容错位）。
- **L5（标签可读）**：边标签加白底 halo（SVG `paint-order:stroke` + 白描边），避免压线；hover 溯源、tooltip 保持现状。

### 4.3 备选方案 B（直列 + 分辨率标尺）

完全去掉 res 缩进：全图单列主轴，左侧画分辨率标尺/色带表示各段 res。优点：绝无偏移、实现最简；缺点：失去 U 型 encoder-decoder 的直观形态（TODO 4 明确要求"清晰看出架构"，U 型是 3D 分割网络最强的架构信号）。**不推荐为默认**，但方案 A 实现后可作为一个渲染开关（res 恒定时两者本就等价）。

### 4.4 与五原则对照

| 原则 | 方案 A 的对应 |
|---|---|
| 层次化 | 泳道坐标逐层继承，展开/折叠语义连续（R2/R3）；聚焦层级不变 |
| 结构化 | 横坐标获得单一全局语义（res），定位规则从 4 套收敛为 1 套（R4） |
| 位置即计算次序 | 纵向 rank 不变；横向不再被 barycenter 平均污染，次序更严格 |
| 走线可溯源不交叉 | L2 分槽消除折线贴线、L3 消除共线、L1 区分方向语义 |
| 通用无特判 | 全部规则只用 DAG + res（追踪形状），无任何架构/模块名判断；res 恒定自动退化直列 |

### 4.5 验收标准与实施拆分（并入原 Step 5，拆为 5a/5b/5c）

- **Step 5a（模块坐标统一，R2/R3/R4）**：改 `computeLayout` 的 cx 计算与容器 res 语义（builder 侧 `_emit_traced:649` 容器 res 改取入口）。验收：① 全折叠时顶层为笔直一列；② 展开 encoder/decoder 后同 res 节点跨层级共竖线（人工截图核验 + 断言 geo 的 x 对齐单测，可用 headless 渲染或把布局函数抽为可测纯函数）；③ 数据流/预测流页渲染与改前一致。
- **Step 5b（观感对齐，R5 + D5/D6）**：宽度统一、续排行左缘对齐、泳道参考线（可选）。验收：截图对比无锯齿错位。
- **Step 5c（走线统一，L1-L5）**：验收：构造扇入/扇出/多车道用例截图，任意两折线不共线不贴线；feedback 与 skip 线型可区分。
- 依赖：5a 是 5b/5c 的前提（坐标变了走线随之变）；三者均不依赖 Step 1-4，可并行，但建议在 Step 3（IR 去几何化）之后做，避免对已删除字段做适配。
- 风险与回退：布局改动无法用现有单测全覆盖，建议每步保留改前 HTML 黄金样本做人工对比；`INDENT` 常量在 res 级很深（如 res5+）时画布过宽，需要给 INDENT 设上限或对深 res 压缩步长（仍是纯数值规则，无特判）。

> **实施后状态更新（2026-07-09）**：Step 1-4 与 5a/5b/5c 已实施。横坐标最终按你的反馈采用**单一主轴**（即 4.3 方案 B 的直列语义：所有容器脊柱严格共一条竖线，res 完全不参与横坐标，仅保留在卡片 in/out 形状中）；4.2 的"res 泳道"缩进已移除，其余 R1/R4/R5 与 L1-L5 规则保持生效。

## 五、EDA（电子电路/PCB 设计软件）借鉴调研

模块布局/布线与 PCB 的器件布局（placement）+ 走线（routing）确实是同构问题：节点=器件、边=网络（net）、容器=模块分区（room/placement group）、车道=布线通道（channel）。EDA 领域对这套问题有 50 年的成熟算法与制图规范，逐项对照如下（★=建议采纳优先级）。

### 5.1 问题映射

| 本可视化 | EDA 对应物 | 现状 |
|---|---|---|
| DAG 节点/卡片 | 器件（component）、原理图符号 | 已有 |
| 边（forward/skip/residual/feedback） | 网络 net；边 kind ≈ **net class**（按类别定线宽/颜色/间距规则） | EDGE_STYLE 已是 net class 思想 |
| 容器（encoder/stage） | 层次化原理图 sheet / PCB room | 已有 |
| 左右车道 + 区间打包 | **通道布线（channel routing）+ 左边算法（left-edge）**：轨道分配即区间图着色 | `allocLanes` 正是贪心左边算法 |
| 主轴直列 | 原理图"信号流从上到下/从左到右"惯例 | 已统一 |

### 5.2 原理图（schematic）制图规范借鉴——观感与可读性，成本低收益高

1. **★★★ 连接点圆点（junction dot）**：原理图铁律——T 型分叉处画实心圆点表示"相连"，十字交叉无点表示"不相连"。现在多扇出（encoder 出口 → 多个 ds_head）的分叉处无标记，与恰好路过的线无法区分。改动小：折点序列里检测分叉点，SVG 画 r≈2.5 的实心圆。
2. **★★★ 跳线标记（wire hop / crossover）**：不可避免的交叉处，其中一条线画半圆"跳"过另一条，明确"交叉但不连接"。与圆点配合后，任意截图都能无歧义读出连接关系——这正是"走线可溯源"的制图学解法。
3. **★★ 网络标签（net label / off-page connector）**：EDA 处理"超长连线"的标准手段——不画实线，在两端放成对命名端子。适用于跨距特别长的跳连（如 stem→decoder.levels.3）：超过阈值（如跨 N 个 rank 或跨 2 级容器）的边退化为成对小标签 `⟨skip:stem⟩`，hover/单击时高亮配对端子并临时画出完整路径。消灭最长的几条左车道线，画布高度深的模型上收益显著；阈值是纯数值规则，无特判。
4. **★ 引脚序（pin ordering）**：符号引脚按连接方向排布以减少交叉。我们卡片入边固定顶缘、出边固定底缘（已符合"输入左/上、输出右/下"惯例），可再进一步：同缘多条边按对端 x 排序分配锚点偏移，消除端口处的局部交叉。
5. **总线归并（bus）**：多条同源同向平行线合并为一条粗总线、两端展开。对 cat 的多输入（如 stems.0/1/2 → cat）可选；但会牺牲逐线溯源，仅在同向平行边 ≥ 4 时值得，优先级低。

### 5.3 PCB 布局（placement）算法借鉴

1. **★★ HPWL（半周长线长）作为量化目标**：EDA placement 的通用目标函数 = 所有 net 包围盒半周长之和。我们行内排序现用 barycenter（前驱均值），可升级为"枚举行内排列取 HPWL 最小"（同 rank 节点通常 ≤6 个，可穷举或贪心交换）；也可作为**布局回归测试指标**——重构后断言黄金样本的总线长不劣化，把"观感"变成可测数字。
2. **分区布局（room/floorplan）**：先按模块划分区域再区内布局——即我们的容器递归布局，思路已一致。EDA 的补充经验：**分区边界预留布线通道**（我们的 gutter），且通道宽度按穿越 net 数预估（我们已按车道数动态算 gutL/gutR，方向正确）。
3. 力导向/模拟退火/解析式 placement：全局优化观感更平滑，但**非确定性**（同图不同解），与"确定性布局、折叠/展开幂等"的既有原则冲突，不采纳。

### 5.4 PCB 布线（routing）算法借鉴

1. **★★ dogleg 换道**：经典通道布线允许一条 net 在中途换轨道（dogleg），可显著减少所需轨道数（车道数↓ → gutter 更窄 → 画布更紧凑）。现在一条边全程占一条车道；跨距很长的边可在中途空闲区间换到更靠内的车道。实现：`allocLanes` 按区间分段分配即可，仍是确定性贪心。
2. **★★ 拥塞感知 + rip-up & reroute**：EDA 先全局布线估计各通道拥塞，再对拥塞段拆线重布。对应我们：当某侧车道数超阈值（如 >5）时，把部分短跨度边改走"层间水平通道"（L2 的分槽）而不是侧缘车道，平衡左右 gutter 宽度。仅在拥塞时触发，规则纯数值。
3. **Lee 迷宫 / A\* 推挤布线**：任意障碍下找最短正交路径，是"线穿卡"问题的彻底解法。但复杂度高、结果对输入敏感；我们现有"直连被挡 → 改走侧车道"的两级策略已覆盖绝大多数情形，暂不引入，作为交叉计数 >0 时的兜底备选。
4. **45° 斜角/圆弧倒角**：PCB 走线避免直角的美学惯例；对可读性提升有限，现有圆角折线已足够。

### 5.5 DRC（设计规则检查）→ 布局自检 ★★★

EDA 最值得搬来的**工程实践**：布完自动跑规则检查而不是靠眼看。在 `computeLayout` 后加一个纯函数 `layoutDRC(geo, routes)`，检查：① 卡片-卡片重叠；② 线段穿越卡片（非端点）；③ 平行线段间距 < 最小值/完全共线；④ 折点落在卡片内。开发模式 `console.warn` 输出违规清单；同时把它接进黄金样本测试（headless 渲染跑 DRC，断言违规数为 0）——布局质量从"截图人审"升级为"可回归的硬指标"，也直接回应 TODO.md"完成与自查"的要求。

### 5.6 交互借鉴

- **cross-probing（双向联动）**：EDA 中原理图与 PCB 双向高亮。我们导航面板→画布已有，补画布→导航反向高亮即可（改动极小）。
- **rat's nest（拉线预览）**：布局调试视图，所有连接画成直线鸟瞰连接密度。可作为开发者开关，验证"位置即计算次序"时一眼看出逆流边。
- net 单击高亮（已有）= EDA 的 net highlight，保持。

### 5.7 采纳建议汇总

| 优先级 | 项 | 改动量 | 对应原则 |
|---|---|---|---|
| P1 | 5.5 DRC 布局自检 + 黄金样本断言 | 中 | 完成与自查、可回归 |
| P1 | 5.2.1 junction dot + 5.2.2 跳线标记 | 小 | 走线可溯源 |
| P2 | 5.2.3 超长边 net label 化 | 中 | 可溯源、画布紧凑 |
| P2 | 5.4.1 dogleg 换道 + 5.4.2 拥塞分流 | 中 | 不交叉、紧凑 |
| P2 | 5.2.4 端口按对端排序 | 小 | 不交叉 |
| P3 | 5.3.1 HPWL 排序/回归指标 | 小 | 结构化 |
| P3 | 5.6 反向联动 / rat's nest 开关 | 小 | 交互 |

不建议采纳：力导向/退火 placement（非确定）、迷宫布线（复杂度）、总线归并（牺牲溯源）、45° 斜线（收益低）。

---

**总结**：审查 5 项结论全部核验成立；独立分析补充 3 个新发现（residual 标签硬编码、RNG 泄漏、head 命名特判——最后一项是五原则中"通用无特判"的唯一违背点）。建议按 Step 1→2→(3,4)→5 顺序执行，1/2/4 改动小风险低，3 有测试联动，5 全部缓做。等你确认后再动手。
