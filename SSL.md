# CT 自监督预训练方案对比 · 候选方法终版描述

> 目标:在 100 例有标注 + 4000 例私有无标注(可叠加公开语料)的设定下,对多种自监督预训练方法做受控对比,逐一作为下游分割/分类的基石出结果。本文给出全部候选方案的统一规格描述,供后续实验逐个实现与横向比较。骨干固定为 CNN,自监督方法覆盖掩码图像建模、自蒸馏、隐空间预测三大范式及其混合。

---

## 0. 对比实验框架与控制原则

### 0.1 变量控制

任何方案不经实验都只是假设。为使结论可信,所有方案必须固定同一套 ①CNN 骨干与容量、②预训练语料与预处理、③算力预算与训练 epoch 数、④下游微调协议与评测划分,**只让"自监督目标"这一个变量变化**。任何额外引入的模块(投影头、预测器、EMA 教师等)都属于该方案的固有组成,不计入"骨干容量"的对比口径。

### 0.2 统一骨干(各方案共用)

取一个 nnUNet 式 3D 编码器,五个分辨率级、总下采样步长 16,归一化用 InstanceNorm、激活用 LeakyReLU。单通道 128³ 体块输入后,在原分辨率以 32 通道起步,随后每级步长 2 下采样,依次经过 64³/64 通道、32³/128 通道、16³/256 通道,直至瓶颈 8³/320 通道。除非方案另有说明,骨干结构一律不改;方案之间的差异只体现在前向方式(稀疏/稠密)、附加模块与训练目标上。下游一律以此编码器为迁移对象。

### 0.3 预训练语料与预处理

统一 spacing(胸部 CT 各向异性明显,in-plane 取 1.0–1.5 mm,z 轴单独处理)、统一朝向(RAS);HU 裁剪后归一化,胸部建议多窗(肺窗利于血管/支气管,纵隔窗利于淋巴结/心脏);随机裁 128³ patch。语料以胸部为主(私有 4000 例 + CT-RATE 等公开集),**务必保留平扫与增强两种相位**及不同重建卷积核的多样性,否则对增强相位与细结构的表征会偏弱。

### 0.4 下游评测协议

预训练完成后,对每个下游任务在固定的留出测试集上评测,并绘制"标注量 vs 性能"的少样本曲线(如 10/30/50/100 例)。分割报 Dice/HD95,分类报 AUC/F1。每个任务同时给出两种迁移读数:线性探针(冻结编码器)与全参微调。**from-scratch 直训必须作为下界基线**(见基线 B2),否则整套对比失去意义。

### 0.5 预训练过程监控

**不要只看预训练损失。** 重建/蒸馏损失低不等于特征好。训练过程中应周期性地在一个小留出标注子集上跑线性探针或快速微调,以表征质量(而非自监督代理损失)作为早停与选型的真实信号。

---

## 方案一 · SparK-3D:稀疏卷积层次化掩码建模

**范式与假设。** 像素重建式掩码图像建模(MIM)的 CNN 原生实现。它验证的假设是:只要用稀疏卷积消除掩码带来的分布偏移,BERT 式"遮挡—重建"就能让卷积网络学到强可迁移的密集特征,而无需改动骨干结构。

**骨干与维度流。** 采用 §0.2 的统一编码器(128³ 输入,32→64→128→256→320 通道,瓶颈 8³)。骨干本身完全不改,仅把前向的稠密卷积替换为稀疏卷积实现(spconv/TorchSparse)。

**掩码生成与多尺度传播。** 在与总步长对齐的单元上掩码——单元取 16³,于是单元网格恰为 8³=512 个,与瓶颈分辨率一一对应。采样一个二值掩码 M∈{0,1}^{8×8×8},mask ratio 取 0.6(SparK 经验值,低于 MAE 的 0.75,因稀疏卷积下信息更难恢复)。在任一编码器尺度,把 M 用最近邻上采样到该尺度,得到该层的激活掩码用于门控位点,使被遮区域在所有尺度上保持空间一致,不随下采样错位或被填满。

**稀疏编码前向(核心机制)。** 仅可见单元转为稀疏张量(坐标+强度)送入网络。每个卷积只在活动位点上运算,且每个 stage 输出后用对应尺度的掩码重新置空,确保感受野不向被遮区域渗透——这正是相对"零填充+稠密卷积"避免分布偏移的根本所在。归一化层只在活动位点上统计(InstanceNorm/GroupNorm/LayerNorm 因按样本统计天然适配;若改用带 BatchNorm 的骨干须替换为稀疏感知 BN,否则零值污染统计量)。编码器全程不引入任何 mask token,这是它区别于 SimMIM 的效率与纯净之处。

**Densify 与层次解码(轻量、用完即弃)。** 解码前先做 densify:把各尺度稀疏特征散射回稠密网格,空位填入该尺度共享的可学习 mask embedding。解码器是一个比编码器窄得多的对称 UNet——从瓶颈 8³(256 通道)起,每个解码块做三线性上采样×2、3×3×3 卷积、归一化与激活,并融合同尺度编码器横向特征,通道随分辨率回升递减(16³/128、32³/64、64³/32),最终在 128³ 输出单通道重建。其参数量约为编码器的 1/5–1/10,预训练后连同所有 mask embedding 一并丢弃。

**目标与损失。** 仅对被遮单元计算。对每个 16³ 单元内体素做归一化目标(减单元均值、除单元标准差,沿用 MAE 经验,稳定优化并削弱 HU 绝对值偏置),损失为这些位置上的 L2:`L = mean over masked voxels of ‖prediction − normalized target‖²`,可见区域不计入。

**稀疏—稠密等价与下游迁移。** 子流形稀疏卷积在满密度输入下严格退化为普通稠密卷积,故"预训练时稀疏前向、下游时稠密前向"共享同一套权重,无需任何转换。下游仅迁移编码器:分割接全新 UNet 解码器(可用本方案解码器结构作初始化),分类接全局池化加 MLP 头;统一全参微调,编码器可配较低学习率或层级衰减。

**关键超参。** mask ratio 0.6;mask unit 16³;输入 patch 128³;AdamW,weight decay 0.04,cosine 调度带 warmup,长 schedule(数百 epoch);全程混合精度与梯度检查点。

**在对比实验中的定位。** "CNN 上正确实现的 MIM"这一类的强基准,单独隔离"稀疏卷积消除掩码分布偏移"这一因素,是方案二 SimMIM-3D 的直接对照系,并为方案八 SparK+DINO 提供 MIM 主干。

---

## 方案二 · SimMIM-3D:稠密卷积 + Mask Token 掩码建模

**范式与假设。** 同为像素重建式 MIM,但走"朴素稠密卷积"路线。它验证的假设是:在 3D CT 上,只需在输入处用 mask token 占位、全网格稠密前向、再接一个极轻的预测头,即可学到有用表征——从而反向检验 SparK 的稀疏卷积机制是否真的必要,以及掩码分布偏移在 3D CT 上的实际代价。

**骨干与维度流。** 沿用 §0.2 的统一编码器,但以**普通稠密模式**运行:编码器始终处理完整 128³ 网格,被遮单元在输入处替换为可学习的 mask token(一个跨该单元广播的可学习向量)。因此其前向算量显著高于 SparK,换来实现简单。

**掩码与前向(核心机制)。** 仍以 16³ 为单元做 patch-wise 掩码,mask ratio 取 0.5–0.6;被遮单元在输入处以 mask token 取代后,整图进入稠密编码器。与 SparK 的本质差异有二:其一,无稀疏剔除,掩码信息在各层都"在场",网络可能感知掩码边界、产生分布偏移;其二,SimMIM 刻意采用极轻的单层预测头,主张让重负担落在编码器而非解码器上。

**预测头与重建。** 取编码器特征,经一个轻量预测头(单个转置卷积或线性投影 + 上采样)直接映射回 128³ 重建强度,**不设跨尺度 skip 连接**——这与 SparK 的层次化解码器形成又一对照变量。

**目标与损失。** 仅对被遮单元计算 L1 损失(SimMIM 原选 L1,与 SparK 的 L2 形成对照点):`L = mean over masked voxels of |prediction − target|`。

**下游迁移。** 迁移编码器,丢弃预测头;分割接全新 UNet 解码器,分类接全局池化加 MLP,全参微调。

**关键超参。** mask ratio 0.5–0.6;mask unit 16³(或更大 32³);轻量单层预测头;AdamW,cosine 带 warmup,长 schedule;混合精度。

**在对比实验中的定位。** SparK 的直接对照系,联合隔离两个变量:稀疏 vs 稠密编码、层次解码器 vs 极简头。若 SimMIM≈SparK,则稀疏机制非必需;若 SparK≫SimMIM,则掩码分布偏移在 3D CT 上确有代价。

---

## 方案三 · Models Genesis:多变换还原式自监督

**范式与假设。** 还原式(restoration)自监督,最 CT 原生、最 CNN 原生。它验证的假设是:让网络从一组手工设计的退化中还原原图,可在不依赖掩码或对比的前提下学到外观、纹理与上下文;且该法本就是为 3D UNet 在胸部 CT 上设计。

**骨干与维度流。** 与其它方案的关键差异:它训练**完整 3D UNet(编码器 + 解码器都训)**,而非 MIM 那样丢弃解码器。编码器仍沿用 §0.2 规格以保证公平,并配一个对称解码器,使下游分割可同时迁移编码器与解码器。

**退化与还原(核心机制)。** 对输入子体块叠加若干随机变换,训练 UNet 还原其原始版本:非线性强度变换(Bézier 曲线映射,学外观/强度)、局部体素置乱(小窗内打乱,学局部纹理与边界)、外补绘(遮外侧预测周边,学全局几何)与内补绘(遮内部预测内容,学局部上下文)。这些变换按概率随机组合。与 MIM 的本质差异在于:退化是多样的手工变换而非单纯掩码,且重建覆盖**整幅子体块**而非仅被遮区域。

**目标与损失。** 还原输出与原始子体块之间的逐体素重建损失(MSE 或 L1),在全体素上计算。

**下游迁移。** 迁移整套 3D UNet(编码器 + 解码器)——这是其独有优势:分割任务的解码器也被预训练过,迁移可能格外稳健;分类任务则取编码器加全局池化与 MLP 头。

**关键超参。** 子体块尺寸(为一致性可取 128³);各变换的触发概率与强度;AdamW 或 SGD,长 schedule。

**在对比实验中的定位。** CNN/CT 原生的还原式基准,检验"多变换还原是否胜过单纯掩码";也是唯一会预训练分割解码器的方案,故其分割迁移可能偏强,即便其纯编码器特征未必最优——一个重要的横向参照。

---

## 方案四 · DINO-3D:图像级自蒸馏

**范式与假设。** 图像/全局级的自蒸馏。它验证的假设是:在教师—学生 EMA 框架下强制跨视图一致,可在不做任何重建的情况下学到全局判别性强的特征(利好分类),从而隔离"蒸馏 vs 重建"这一轴在全局层面的差异。

**骨干与附加模块。** 沿用 §0.2 编码器(稠密模式),其后接一个投影头(MLP 映射到高维"原型"logits)。无解码器。存在两份网络:学生(梯度更新)与教师(学生的 EMA)。

**多视图与蒸馏(核心机制)。** 对每个体块采样若干全局裁剪(大视野,约 128³)与多个局部裁剪(小视野,约 64³)。两网络分别编码→全局池化→投影头→带温度的 softmax。教师只看全局裁剪,学生看全部裁剪;损失要求"学生在每个裁剪上的分布"逼近"教师在全局裁剪上的分布",对所有"学生裁剪×教师全局"配对求和。防坍缩靠三件套:教师输出中心化(减去滑动均值)、锐化(教师温度更低)、教师 EMA 更新。裁剪增强须 CT 语义安全(限幅内的强度缩放/平移、gamma、噪声、模糊、翻转、90° 旋转),避免破坏区分平扫/增强所需的 HU 语义。

**目标与损失。** 中心化、锐化后的教师 softmax 与学生 softmax 间的交叉熵,按裁剪配对求和。

**下游迁移。** 迁移编码器,丢弃投影头与教师;分割接全新 UNet 解码器,分类接全局池化加 MLP。注意 DINO 特征常在分类线性探针上偏强、在密集预测上偏弱,分割宜全参微调。

**关键超参。** 局部裁剪数(约 6–8);裁剪尺寸;教师温度(0.04→0.07 warmup),学生温度 0.1;中心化动量 0.9;EMA 动量 0.996→1.0(cosine);投影维度。

**在对比实验中的定位。** 纯全局自蒸馏,隔离"图像级蒸馏是否、以及多大程度上有助于你的分类任务",并量化其在分割上的不足;是方案五(Gram)与方案六(混合)的基线。

---

## 方案五 · DINO-3D + Gram Anchoring:抑制密集特征退化

**范式与假设。** 在方案四基础上加入 DINOv3 的 Gram anchoring,以修复长 schedule 下密集特征退化的问题。它验证的假设是:约束 patch 特征的 Gram 矩阵可保留空间/密集结构,使自蒸馏在分割上也具竞争力,而不只擅长分类。

**骨干与附加模块。** 与方案四完全相同。额外定义"密集特征"为编码器在某尺度(如瓶颈 8³ 或某中间层)**全局池化之前**的空间特征图;并维护一个"Gram 教师"——通常取较早期、密集特征尚佳的教师快照,专用于 Gram 目标,周期性刷新。

**Gram anchoring(核心机制)。** 在 DINO 图像级损失之外,对学生与 Gram 教师各自计算 L2 归一化密集特征的 Gram 矩阵 `G = X·Xᵀ`(X 为 位置数×通道 的归一化特征矩阵),G 捕捉空间位点两两相似度。Gram 损失为两矩阵的 Frobenius 距离 `‖G_student − G_gramteacher‖²`,促使学生保留教师的空间相似度结构,从而在长训练中维持密集特征的锐利。

**目标与损失。** `L_total = L_DINO + λ·L_gram`,λ 为权重(取较小值),通常在若干 warmup epoch 后再开启 Gram 项。

**下游迁移。** 同方案四。

**关键超参。** Gram 权重 λ;Gram anchoring 起始 epoch;Gram 教师刷新间隔;用于 Gram 的特征尺度。其余同方案四。

**在对比实验中的定位。** 相对方案四只切换"Gram anchoring 开/关"这一个变量,检验 DINOv3 的核心技术能否在 3D CT 上挽救自蒸馏的密集/分割迁移。

---

## 方案六 · iBOT/DINOv2 式混合:全局蒸馏 + 掩码特征预测

**范式与假设。** 将图像级自蒸馏与 patch 级掩码特征预测(iBOT 目标;DINOv2 = DINO + iBOT)结合。它验证的假设是:在 DINO 之上叠加一个"在特征空间预测被遮位点特征"的目标,可在单一模型内同时获得全局判别性与密集特征。注意它属于隐空间预测思路,但与蒸馏耦合,区别于纯 JEPA。

**骨干与附加模块。** 沿用 §0.2 编码器。因 CNN 不能丢 token,掩码分支用 SparK 式稀疏输入或 SimMIM 式 mask token 输入实现:学生看被遮输入,教师看完整输入,二者各出密集特征图;另设 DINO 图像级头作用于全局池化特征。教师为 EMA。

**两路目标(核心机制)。** (a) DINO 全局损失:多视图下对全局池化特征做自蒸馏(同方案四)。(b) iBOT 掩码特征损失:遮住学生输入的若干单元,对每个被遮位点,要求学生投影后的密集特征匹配教师在该位点的密集特征——以共享原型上的逐位点 softmax 交叉熵实现,同样配中心化与锐化。两头可共享或独立原型。

**目标与损失。** `L = L_DINO(global) + L_iBOT(masked dense, 特征空间交叉熵)`。

**下游迁移。** 同方案四;若训练稳定,预期是分类与分割双强的单一模型。

**关键超参。** iBOT 分支掩码比(约 0.3–0.5);多视图配置;两路温度;原型维度;两项损失权重。

**在对比实验中的定位。** "两全其美"候选,但复杂度与坍缩风险最高;检验"全局蒸馏 + 密集掩码特征预测"是否胜过任一单项。直接对照:方案四(仅全局)、方案一(仅像素掩码)、方案七(仅隐空间掩码、无蒸馏)。

---

## 方案七 · JEPA-3D:隐空间掩码预测

**范式与假设。** 联合嵌入预测架构(JEPA)——在隐空间预测被遮区域的表征,不做像素重建、不做实例对比。它验证的假设是:预测特征(而非像素)迫使模型聚焦高层语义、丢弃不可预测的低层噪声(HU 散斑、卷积核相关纹理),可能更契合 CT。它隔离的关键轴是:**隐空间目标 vs 像素目标**(对照方案一/二)。

**骨干与附加模块。** 同一编码器同时充当上下文编码器与(EMA)目标编码器,另加一个轻量预测器。I-JEPA 原生为 ViT(上下文/目标 ViT + token 上的预测器),CNN 适配方式如下:目标编码器(EMA)编码完整体块得密集目标特征图,从中选取若干连续目标块的特征作为预测目标;上下文编码器编码"遮去目标块后的上下文区域"(以稀疏或 mask token 输入实现);预测器(轻量卷积网或小型 transformer)依据上下文特征与目标块位置信息,预测该目标块的特征。

**预测与防坍缩(核心机制)。** 损失为预测特征与目标编码器特征之间的 L2(对目标侧 stop-gradient):`L = mean ‖predictor(context, pos) − sg(target_encoder(block))‖²`。防坍缩依靠:目标侧 EMA(不接收梯度)、非对称预测器,并可叠加 VICReg 式方差—协方差正则(脑 MRI JEPA 工作即加入此项)以避免表征塌缩。掩码策略可借鉴 I-JEPA 的"少量大目标块 + 大上下文块",CT 上亦可用结构/解剖优先的掩码(参考 Seg-JEPA)。

**目标与损失。** 上述隐空间 L2,选配 VICReg 的方差与协方差项。

**下游迁移。** 迁移上下文编码器(或目标编码器);分割接全新 UNet 解码器,分类接全局池化加 MLP。

**关键超参。** 目标块数量与尺寸;上下文范围;预测器深度;EMA 动量;VICReg 权重(若用)。

**在对比实验中的定位。** 第三范式——隐空间掩码预测。直接对照方案一(同为掩码思路但像素目标)与方案六(隐空间掩码预测但耦合蒸馏与原型),干净地检验"避开像素重建"在噪声较大的 CT 上是否有益。

---

## 方案八 · SparK + DINO:像素掩码主干 + 全局蒸馏

**范式与假设。** 以多任务相加的简单方式,把像素 MIM(SparK,密集强)与图像级蒸馏(DINO,全局强)结合,力图在不引入 iBOT 复杂度的前提下,得到一个分割与分类双强的 CNN 模型。它验证的假设是:一个务实的双损失组合能在混合任务集上胜过任一单项。

**骨干与附加模块。** 沿用 §0.2 编码器,由两分支共享:SparK 分支(稀疏掩码输入→编码器→层次解码器→像素重建,同方案一)与 DINO 分支(完整/多视图输入→编码器→全局池化→投影头→对 EMA 教师蒸馏,同方案四)。编码器在 SparK 分支走稀疏、在 DINO 分支走稠密,由稀疏—稠密权重等价无缝支持。

**联合训练(核心机制)。** 每步(或交替)同时计算 SparK 重建损失与 DINO 蒸馏损失,在相应视图上回传二者之和;教师由学生 EMA 更新。DINO 分支用多视图,SparK 分支用单一被遮视图。

**目标与损失。** `L = L_SparK(像素重建) + μ·L_DINO(全局蒸馏)`,μ 为权重。

**下游迁移。** 同前;迁移编码器,分割接全新 UNet 解码器,分类接全局池化加 MLP。

**关键超参。** 损失权重 μ;联合 vs 交替;裁剪配置;mask ratio;教师温度等。

**在对比实验中的定位。** 务实混合方案,工程风险低于方案六;检验"朴素多任务(MIM + 蒸馏)"能否捕获大部分收益。对照:方案一(仅 MIM)、方案四(仅蒸馏)、方案六(更紧的隐空间级整合)。

---

## 基线 B1 · 3D 对比学习(BYOL-3D / MoCo-3D)

**范式与假设。** 实例判别式对比学习,作为蒸馏家族的下界参照。它验证:经典对比目标能否、以及多大程度上接近自蒸馏的全局表征质量。

**骨干与机制。** 沿用 §0.2 编码器加投影头(BYOL 另加预测头)。BYOL:对同一体块取两个增强视图,在线网预测目标网(EMA)的投影,无需负样本,目标侧 stop-gradient;MoCo:以动量队列维护负样本做对比。增强须 CT 语义安全。

**目标与损失。** BYOL 为预测—目标间的余弦/L2 损失;MoCo 为带动量队列的 InfoNCE。

**下游迁移。** 迁移编码器;分割接全新 UNet 解码器,分类接全局池化加 MLP。

**定位。** 经典对比基线,预期全局/分类尚可、密集/分割偏弱,是自蒸馏家族的比较地板。

---

## 基线 B2 · From-scratch(无预训练)

**范式与假设。** 不做任何预训练的必备对照。它定义了所有自监督方法必须跨过的及格线。

**机制。** 用与各方案相同的编码器/解码器、相同增强、相同 schedule,直接在 100 例有标注数据上分任务训练。

**定位。** 全套对比的基准下界;离开它,"预训练是否有用"无从判定。所有方案的收益都应相对它来度量,并在少样本曲线上观察预训练优势随标注量增加的衰减趋势。

---

## 附:跨方案一览对照

| 方案 | 范式 | 预测/监督目标 | 是否预训练解码器 | CNN 适配关键 | 预期偏强项 | 主要隔离的变量 |
|---|---|---|---|---|---|---|
| ① SparK-3D | 像素掩码 | 被遮体素强度 | 否(弃用) | 稀疏卷积 | 分割/密集 | 稀疏消除分布偏移 |
| ② SimMIM-3D | 像素掩码 | 被遮体素强度 | 否(极简头) | mask token 稠密 | 分割/密集 | 稀疏 vs 稠密、解码器繁简 |
| ③ Models Genesis | 还原 | 多变换还原全图 | 是(整 UNet) | 原生 3D UNet | 分割(解码器已训) | 多变换还原 vs 掩码 |
| ④ DINO-3D | 自蒸馏 | 全局分布一致 | 否 | 全局池化 + 投影头 | 分类/全局 | 蒸馏 vs 重建 |
| ⑤ DINO+Gram | 自蒸馏 | 全局 + Gram 约束 | 否 | 密集特征 Gram | 分类 + 改善分割 | Gram anchoring 开/关 |
| ⑥ iBOT/DINOv2 式 | 蒸馏 + 隐空间掩码 | 全局 + 被遮位点特征 | 否 | 掩码输入 + 双头 | 分类 + 分割 | 全局 + 密集掩码联合 |
| ⑦ JEPA-3D | 隐空间掩码 | 被遮块特征 | 否 | 上下文/目标/预测器 | 语义/抗噪 | 隐空间 vs 像素目标 |
| ⑧ SparK + DINO | 像素掩码 + 蒸馏 | 体素强度 + 全局分布 | 否 | 稀疏/稠密双分支 | 分割 + 分类 | 朴素多任务组合 |
| B1 BYOL/MoCo | 对比 | 视图一致/InfoNCE | 否 | 投影(+预测)头 | 分类/全局 | 对比 vs 蒸馏 |
| B2 From-scratch | — | — | — | — | — | 预训练净收益的下界 |

> 实施建议(不属方案描述,仅供排期参考):先把方案一/二/三/四(每个范式各一个代表)跑通并锁定下游评测管线,确认各自能否超过 B2;再上方案五/七(单一新变量)与方案六/八(混合)。混合与隐空间方案的坍缩风险与调参成本最高,放在管线稳定之后。
